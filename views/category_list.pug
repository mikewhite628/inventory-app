extends layout

block content
  h1= title
  p Welcome to #{title}

  if error 
    p error loading items 
  else 
    dl
      each item in categories
        dt 
          img.avatar(src='/images/'+item.image)
          a(href=item.url) #{item.name}

extends layout 

block content 

  h1 #{title}: #{item.name}

  p Do you really want to delete this item?

  form(method='POST' action='')
    div.form-group
      input#item.id.form-control(type='hidden', name='itemid', required='true', value=item._id)
    button.btn.btn-primary(type='submit') Delete